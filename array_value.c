#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int p,i,c;
  int arr[10]={3,34,5,6,7,86,5,4,8,2};
	p=987;
	for( i=1;i<10;i++)
	{ 
	if(p=arr[i])
	 {
	 c=1;
}

 


	if(c==1)
	printf("found");
	else
	printf("not found");
	getch();
	return 0;
}
}
